// First non-repeating character

// Goal: First character that appears only once.

// Test: Hash maps for counting frequency.

function nonRepeatChar(arr){
for(let char of arr){
    if(!char.include(arr)){
return char
    }
}
}
console.log(nonRepeatChar('aanarnrpjj'));
